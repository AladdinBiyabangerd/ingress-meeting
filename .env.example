# Copy to .env and fill in before docker compose up
OPENAI_API_KEY=
API_SECRET_KEY=
# Academy webhook base (no trailing slash). Meeting POSTs {base}/{jobId}/complete
# Example: https://ingress.academy/portal/api/meeting/jobs
ACADEMY_CALLBACK_BASE_URL=
# whisper.cpp ggml model (default quantized large-v3 for CPU speed)
# Options: large-v3-q5_0 | large-v3-q8_0 | large-v3 | medium | small | base
WHISPER_MODEL=large-v3-q5_0
# 0 = all CPU cores
WHISPER_THREADS=0
JAVA_OPTS=-Xms512m -Xmx2g
